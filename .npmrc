# exFAT volume shared with macOS: no symlink guarantees on Windows -> flat node_modules.
node-linker=hoisted
# We manage installs explicitly; avoids the build-script-approval prompt blocking dev/build.
verify-deps-before-run=false
